// Re-export from the existing AI module so all service imports
// come from the same src/services/ namespace.
export { analyzeIssue, generateContractorBrief, summarizeInspection } from '../../ai.js';
